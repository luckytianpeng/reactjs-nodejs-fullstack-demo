const styles = theme => ({
  root: {
    magin: 0,
    padding: 0,
  },
  small: {
    width: theme.spacing(3),
    height: theme.spacing(3),
    borderRadius: '5px',
    marginTop: 0,
    marginBottom: 0,
    marginLeft: 0,
  },
});

export { styles };
